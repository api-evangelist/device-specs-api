generated: '2026-08-09'
method: generated
source: openapi/device-specs-api-openapi-original.json
searched_first:
  - {url: "https://ds.gtgroup.dev/agents.md", http_status: 404}
  - {url: "https://ds.gtgroup.dev/llms.txt", http_status: 200, note: "llms.txt is documentation context, not a packaged agent skill"}
note: >-
  The provider publishes no AGENTS.md or packaged skill, so these are generated by API Evangelist
  from the real published operations. The OpenAPI declares NO operationIds, so each step is grounded
  in the verbatim method + path from the spec; the parenthesised ids are the operationIds proposed in
  overlays/device-specs-api-openapi-overlay.yaml.
skills:
  - file: device-specs-api-lookup-device-specs.md
    name: Look up the full specification for one phone
    api: openapi/device-specs-api-openapi-original.json
    operations:
      - "GET /api/values/clean/getspecs/{manufacturer}/{model}"
      - "GET /api/values/clean/getdevices/{manufacturer}"
  - file: device-specs-api-filter-devices-by-hardware.md
    name: Find devices matching hardware criteria with the deep filter engine
    api: openapi/device-specs-api-openapi-original.json
    operations:
      - "GET /api/values/clean/devices"
  - file: device-specs-api-compare-chipset-cohort.md
    name: Compare every device on one chipset
    api: openapi/device-specs-api-openapi-original.json
    operations:
      - "GET /api/values/clean/devicesbychipset/{chipset}"
      - "GET /api/values/clean/getspecs/{manufacturer}/{model}"
